# Extra dependency for the MCP server (kwara/mcp_server.py).
# The CLI (kwara/cli.py) needs nothing beyond requirements.txt — install this
# only if you want to drive kwara from an MCP client such as Claude Code.
-r requirements.txt
mcp>=1.2.0
